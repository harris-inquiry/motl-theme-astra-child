{
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "postcss": "^8.3.6",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.7"
  },
  "scripts": {
    "devwatch": "postcss src/*.css --dir assets/css --watch",
    "devbuild": "postcss src/*.css --dir assets/css",
    "build": "NODE_ENV=production npm run devbuild"
  }
}
